use uuid::Uuid;

// Estrutura para o usuário
#[derive(Debug)]
pub struct User {
    id: i32,
    username: String,
    password: String,
}

impl User {
    fn CreateUser() {
      
    }
}
